pipeline {
    agent any

    stages {

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t python-web .'
            }
        }

        stage('Run Container') {
            steps {
                sh 'docker run -d -p 6000:6000 python-web'
            }
        }

    }
}
